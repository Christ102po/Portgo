# Leave VITE_API_BASE_URL unset in production when frontend + API are served
# together by the Express server. For local development the app automatically
# uses http://<current-host>:4000/api.
# VITE_API_BASE_URL=https://api.example.com/api

VITE_PORT_NAME="Surigao"
VITE_PORT_DESTINATIONS="Dapa"
