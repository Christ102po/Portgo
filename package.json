{
  "name": "portgo-system",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm ci --prefix backend && npm ci --prefix frontend",
    "build": "npm run install:all && npm run prisma:generate --prefix backend && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "db:push": "npm run prisma:push --prefix backend",
    "seed": "npm run seed --prefix backend"
  }
}
